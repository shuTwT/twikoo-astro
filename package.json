{
  "private": "true",
  "repository": "git@github.com:shuTwT/astro-twikoo.git",
  "author": "shu <1324073644@qq.com>",
  "license": "MIT",
  "type": "module",
  "scripts": {
    "dev": "pnpm run --filter demo dev",
    "build": "pnpm run --filter core build",
    "build:locale": "pnpm run --filter locale build",
    "build:marked": "pnpm run --filter marked build",
    "clean": "pnpm run clean:dist",
    "clean:dist": "rimraf dist",
    "stub": "pnpm run -r --parallel stub"
  },
  "devDependencies": {
    "tsup": "^7.2.0",
    "typescript": "^5.0.4",
    "vite": "^4.5.0"
  }
}
